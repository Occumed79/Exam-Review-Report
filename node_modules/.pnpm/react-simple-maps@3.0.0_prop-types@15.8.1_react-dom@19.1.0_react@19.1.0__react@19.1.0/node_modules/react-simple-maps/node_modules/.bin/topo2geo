#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*|*MINGW*|*MSYS*)
        if command -v cygpath > /dev/null 2>&1; then
            basedir=`cygpath -w "$basedir"`
        fi
    ;;
esac

if [ -x "$basedir/node" ]; then
  exec "$basedir/node"  "$basedir/../../../topojson-client/bin/topo2geo" "$@"
else
  exec node  "$basedir/../../../topojson-client/bin/topo2geo" "$@"
fi
# cmd-shim-target=/home/ubuntu/work/Exam-Review-Report/node_modules/.pnpm/react-simple-maps@3.0.0_prop-types@15.8.1_react-dom@19.1.0_react@19.1.0__react@19.1.0/node_modules/topojson-client/bin/topo2geo
